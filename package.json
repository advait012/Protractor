{
  "name": "protractor-new",
  "dependencies": {
    "protractor": "^5.4.2",
    "jasmine-data-provider": "^2.2.0"
  },

  "scripts":{
      "protractor": "./node_modules/.bin/protractor configuration.js",
       "we": "./node_modules/.bin/webdriver-manager update",
"start": "npm run we &&npm run protractor"
  }
}
